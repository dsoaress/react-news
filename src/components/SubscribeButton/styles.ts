import tw, { styled } from 'twin.macro'

export const Wrapper = styled.button`
  ${tw`flex items-center justify-center h-16 text-xl font-bold transition duration-200 rounded-full w-72 bg-primary-500 hover:(filter brightness-90)`}
`
